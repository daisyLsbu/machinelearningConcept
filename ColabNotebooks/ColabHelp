Go to https://colab.research.google.com
File → New notebook in Drive: it creates a colab folder in your drive and upload it there
create a folder for files and data called Collaborate
Configure your colab:
{
mount google drive and authenticate to access files
from google.colab import drive
drive.mount("/content/gdrive")
check the currect directory content
! ls
change directory to specific dir
%cd gdrive/MyDrive/'Collaborate '
install any package if missing
! pip install <desired-python-library>
add a file in the data folder to test the access.
}

to run any jupyter notebook int he colab, if it needs to access any data file : add the configuration code in the notebook 
copy the data in the data folder created by you
To run any jupyter notebook -> goto file-> upload file -> browse file and upload
or select from already uploaded files

how to setup GPU in colab:
1. Enable GPU for your notebook
Go to Runtime → Change runtime type (in the top menu)
Under Hardware accelerator, select GPU
Click Save

. Verify it's actually available in your code

Before running any training, always check:

python
import torch
print(torch.cuda.is_available())
print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else "No GPU")
Use the safe device pattern
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

